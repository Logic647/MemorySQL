# 截图清单(发布前手动补齐)

> **当前状态(2026-09-13,自动化截屏完成):**
> - ✅ 已就位:`01-context-handoff` `02-sessions` `03-session-detail` `04-memories` `06-quicksearch` `07-connect-wizard`(在 `design/screenshots/`)
> - 🟦 **01 的内容是演示数据**(虚构项目 caffeine-tracker):通过 `MEMORYSQL_DATA_DIR` 临时目录 + 合成 codex 会话 + MCP `memory_write` 种入的画像/记忆,真实调用本机 MCP 服务端截取——**可公开发布,无隐私内容**。真实库的续接包含学校/服务器 IP 等隐私,不要用真实库截这张
> - ⚠️ **02/03/04/06/07 用的是真实库**:已预检(无 key/IP/学校信息),但含真实会话标题与项目名(如「money」漏洞赏金技能审查),发布前自己过目一遍
> - ⚠️ 五张真实库截图的**顶栏有「导入对话」按钮**——import-chat 尚未随版本发布(tag 后提交)。可接受(发帖时 0.5.1 可能已带)或从 v0.5.0 tag 重建后重截
> - ❌ **05 图谱放弃**:真实库仅 13 节点/1 链接,且节点标题渲染的是 auto-devlog 的 HTML 注释;等笔记多了再截(各渠道分配本就没用到它)
> - ⏳ **08 项目日志待你手动截**:用 Obsidian/编辑器打开 `vault/devlog/<项目>.md` 截一张即可(仅掘金版引用,可选)

---

> 所有帖子与 README 都引用下面这套命名。截图放到 **`design/screenshots/`**(目录不存在就新建),命名必须与清单完全一致,否则 README/帖子里的图挂链。
>
> 通用要求:窗口最大化,1920×1080,显示缩放 100% 或 125%(全系列统一);PNG 格式;截图前过一眼画面里有没有 API key、token、不想公开的真实会话内容,有就换演示数据或打码。

## 必截(8 张)

| # | 文件名 | 画面 | 怎么进 | 状态要求 |
|---|---|---|---|---|
| 01 | `01-context-handoff.png` | **主打图**:agent 终端里的 MCP 续接包实况 | 在已连接 MemorySQL 的 agent(如 ZCode / Codex)开新会话,输入「续接 MemorySQL」 | agent 实际调用 `memory_get_context`,终端展开返回内容(画像/记忆/项目/最近会话/上一棒交接摘要);窗口拉宽、字号调大,让返回文本清晰可读。这张是全组最有说服力的,值得多试几次挑最佳 |
| 02 | `02-sessions.png` | 主界面 · 会话列表 | 启动应用 → 会话页 | 列表里有多家 agent 的会话(badge 颜色区分),项目分组清晰;挑消息数多、标题自然的会话排在上面 |
| 03 | `03-session-detail.png` | 会话详情 · 消息时间线 | 从会话页点开一个内容丰富的会话 | 能看到 user / assistant / TOOL 消息交替,滚动到有工具调用的位置 |
| 04 | `04-memories.png` | 记忆页 | 左侧切到记忆 | 有几条不同 kind(画像/偏好/决策/事实)且带 agent 归因的记忆;内容别敏感 |
| 05 | `05-graph.png` | 知识图谱 | 笔记 → 图谱 | 节点 ≥ 20;先点「重新布局」多试几次,挑节点不堆叠的舒展状态 |
| 06 | `06-quicksearch.png` | 托盘秒搜 | 任意界面按 `Alt+Shift+M` | 输入一个关键词、命中结果已出现时截;搜索词选一个能同时命中会话和记忆的 |
| 07 | `07-connect-wizard.png` | 设置 → 连接 Agent 向导 | 设置 → 连接 Agent | 显示 agent 列表(10 家)或「一键写入配置」步骤界面 |
| 08 | `08-project-log.png` | 自动项目日志 | 用编辑器/Obsidian 打开 `vault/devlog/<项目>.md` | 时间线 + 决策 + 待办三个部分都可见;文件选内容最丰富的那个项目 |

## 可选加映

| 文件名 | 画面 | 说明 |
|---|---|---|
| `09-tray.png` | Windows 托盘弹出区 | 展示常驻图标;拍全弹出气泡更佳 |
| `10-import-chat.png` | 导入对话弹窗 | **注意:该功能尚未发版,首发物料不要用这张**,下个版本发后再补 |

## 各渠道用图分配

| 渠道 | 用图 |
|---|---|
| README | 01、02(嵌图代码已写好,图放入即显示) |
| V2EX | 01、02(首楼最多贴 2 张,别刷屏) |
| 掘金 | 01、02、08 |
| 少数派 | 02、04、06(体验向,少终端) |
| 即刻 | 02、06 |
| MCP 目录 | 不需要图 |
